$(function() {

/*
    //点击详情按钮触发的事件
    $(".con_xianging").click(function() {


        //获取到相关项目的id号
        var id=$($($(this).parent()).children());

        alert($($($(this).parent()).children()).eq(0).text());

    });

    //点击下一步按钮触发的事件
    $(".con_next").click(function() {

        alert($($($(this).parent()).children()).eq(0).text());

    });

    $(".M-box").pagination({
        totalData:700,
        showData:10,
        */
/*pageCount:10,*//*

        jump:true,
        coping:true,
        homePage:'首页',
        endPage:'末页',
        prevContent:'上页',
        nextContent:'下页',
        callback:pageback
    });


    function pageback(index,jq) {
        alert(index.getCurrent());

    }
*/


});